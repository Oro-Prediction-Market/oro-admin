/**
 * Oro's platform fee (a.k.a. "house edge"), in percent — the single source of
 * truth for the admin app. Must match the backend's DEFAULT_HOUSE_EDGE_PCT.
 * Standardised to a flat 10% for all markets (boss review #6).
 */
export const DEFAULT_HOUSE_EDGE_PCT = 10
